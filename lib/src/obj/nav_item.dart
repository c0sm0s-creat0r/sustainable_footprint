import 'package:flutter/material.dart';

/// A placeholder class that represents an entity or model.
class NavItem {
  const NavItem(this.id, this.page, this.icon);

  final String id;
  final String page;
  final AssetImage icon;
}
